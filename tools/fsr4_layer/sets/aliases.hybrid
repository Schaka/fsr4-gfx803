# Tier names for the hybrid build, which leaves ten model passes to AMD's shaders so the sets can
# replace them. `off` is already exact here, because every shader the fork does replace is exact.
lossless=off
quality=fin15
balanced=fin25
speed=prune16
